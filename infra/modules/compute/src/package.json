{
  "name": "tickets-handler",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda handler for Ticke-T REST API + WebSocket chat. Depends on aws-sdk/s3-request-presigner because the Node 22 Lambda runtime ships @aws-sdk/client-s3 and @aws-sdk/client-dynamodb but NOT the presigner package.",
  "dependencies": {
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  }
}
